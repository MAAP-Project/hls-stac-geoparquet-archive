{
  "name": "hls-stac-geoparquet-archive",
  "version": "0.3.0",
  "description": "AWS CDK app for generating a STAC Geoparquet archive of HLS granules",
  "main": "lib/app.js",
  "scripts": {
    "build": "tsc",
    "cdk": "cdk",
    "deploy": "npm run build && cdk deploy",
    "synth": "npm run build && cdk synth"
  },
  "keywords": [
    "aws",
    "cdk",
    "batch",
    "hls",
    "stac",
    "parquet"
  ],
  "author": "hrodmn",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "aws-cdk": "^2.1133.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.260.0",
    "constructs": "^10.0.0"
  }
}
